# Pendências Reais — 31/08/2026 (atualizado, Rodada 17 — 02/09/2026: confirmação automática da Etapa 1, sem clicar em "Iniciar análise documental"; Rodada 18 — 02/09/2026: validação local sem IA/orientação de documento correto/menos texto repetido; Rodada 19 — 02/09/2026: sincronização automática de CNPJ; Rodada 20 — 02/09/2026: Cartão CNPJ confirma e trava a situação cadastral contra a reversão automática; Rodada 21 — 02/09/2026: leitura automática sem clique, falso positivo de nome para Empresário Individual, telefone/e-mail via Cartão CNPJ; Rodada 22 — 02/09/2026: refinamento com os documentos reais, janela de 5 dias para correção de cadastro, trava de edição manual; Rodada 23 — 02/09/2026: leitura visível ao anexar Cartão CNPJ/QSA/Enquadramento; Rodada 24 — 02/09/2026: falha já pendente/travada passa a se resolver sozinha na tela, sem F5; Rodada 25 — 02/09/2026: todos os campos do checklist sempre visíveis, para qualquer empresa/regime; Rodada 26 — 02/09/2026: Cartão CNPJ também corrige o nome empresarial/razão social desatualizado na API gratuita; Rodada 27 — 02/09/2026: botão "Reler" manual em cada card da Etapa 1; Rodada 28 — 02/09/2026: grade de campos ilegível corrigida no Acervo Documental, botão "Reler" do Contrato Social confronta contra o Ato da Junta; Rodada 29 — 02/09/2026: auditoria própria de consistência visual/diagnóstica entre todos os tipos de empresa, três inconsistências corrigidas; Rodada 30 — 02/09/2026: cards do Acervo Documental nivelados quando fechados; Rodada 08/09/2026: identidade documental por evidência em Atos da Junta/Contrato Social, atualizada em 09/09/2026 com o terceiro documento real; Rodada 09/09/2026: laudo por documento vira ícone de hover, cards sempre do mesmo tamanho; Rodada 09/09/2026 parte 2: PDF institucional e roteamento do Dossiê de Crédito; Rodada 09/09/2026 parte 3: cards recolhidos por padrão; Rodada 09/09/2026 parte 4: titular de Empresário Individual/MEI criado automaticamente, destrava documentação pessoal sem QSA; Rodada 09/09/2026 parte 5: correção real chegou na rota que a tela usa -- a parte 4 tinha corrigido só o dossiê; Rodada 09/09/2026 parte 6: causa raiz real encontrada -- isEmpresaIndividual não reconhecia "Empresário (Individual)" com parênteses, confirmado com os 3 documentos reais da empresa; Rodada 09/09/2026 parte 7: MEI usa o CCMEI como equivalente do Contrato Social/Atos da Junta -- Etapa 2 só avança com o CCMEI de fato anexado; Rodada 09/09/2026 parte 8: CCMEI anexado no campo "Contrato social" deixa de ser "Documento incompatível"; Rodada 09/09/2026 parte 9: corrigido o caminho de leitura do tipo detectado -- a etapa ainda pedia o CCMEI mesmo depois da parte 8; Rodada 09/09/2026 parte 10: três textos de tela/mensagem que ainda diziam "Atos da Junta Comercial" para o MEI, corrigidos com o novo campo `empresa_identificada_mei`; Rodada 10/09/2026 parte 11: campo "CCMEI" reordenado para logo após "Contrato social", para MEI -- estava 24 campos abaixo, causando confusão sobre onde anexar; Rodada 10/09/2026 parte 12: para MEI, "Atos da Junta Comercial"/"Contrato social" somem da grade -- CCMEI vira o primeiro campo, reversível por natureza; mais contraste no título dos cards; Rodada 10/09/2026 parte 13: ECF/ECD/DEFIS/DASN-SIMEI respeitam o prazo legal de entrega antes de virarem pendência obrigatória; Rodada 10/09/2026 parte 14: causa raiz real de "o CCMEI nunca aparece na tela" -- faltava nas listas `tiposPermitidos` das duas telas de Acervo Documental de empresa; Rodada 11/09/2026 parte 15: CCMEI preso em "Revisão necessária" -- novo leitor local especializado; Rodada 11/09/2026 parte 16: CCMEI anexado vira evidência direta de MEI no banner de regime e no checklist)

## Convenção de registro (09/09/2026, pedido explícito do usuário)

A partir de agora, toda pendência nova registrada aqui diz de forma curta e direta O QUE é a pendência -- o dado que falta, o documento que falta, a comparação que não bate, o que falta construir -- sem parágrafo de análise longa. Pendências registradas antes desta data mantêm o formato anterior (mais detalhado), para não reescrever histórico sem necessidade.

## 0-AK. [RESOLVIDO NESTA RODADA, 11/09/2026 parte 16, pedido explícito do usuário: "garanta que quando identificar o tipo de empresa, o regime, o enquadramento tributário, garanta que a documentação seja exatamente o que é pra ser [...] só precise da documentação pra anexar no enquadramento do MEI"] Banner de regime mostrava "Simples Nacional — optante" (e o checklist exigia PGDAS-D/DEFIS) para uma empresa MEI real já com CCMEI anexado

`identificarRegimeCredito` só reconhecia MEI quando `opcao_mei` da Receita era `true` ou o texto do enquadramento mencionava literalmente "MEI"/"SIMEI" -- uma empresa com CCMEI anexado, mas cujo enquadramento sincronizado ainda não trazia esse texto (comum logo após a sincronização automática de CNPJ), continuava sendo tratada como Simples Nacional genérico, mesmo com a prova documental definitiva (o próprio CCMEI) já anexada. Corrigido com um terceiro parâmetro opcional `evidenciaCcmeiAnexado`, ligado nos dois pontos que alimentam o banner e o checklist visível (`gerarMapaDocumentalCredito`, `avaliarProntidaoIdentidadeCnpj`), reaproveitando o sinal `ccmei_anexado` que `montarValidacaoSocietaria` já calcula. Deliberadamente não usa natureza jurídica "Empresário Individual" como evidência (removido de propósito na Rodada 29 -- nem todo Empresário Individual é MEI); o CCMEI é seguro porque é o próprio documento que comprova o MEI. Um `opcao_mei: false` explícito nunca é sobrescrito. Ver `tests/regimeCcmeiComoEvidencia.test.ts` (6 testes). Também auditada a "Matriz Estratégica de Documentação para Crédito Empresarial" fornecida pelo usuário: nenhum outro gap de regra de regime encontrado -- a exclusão de PGDAS-D/DEFIS/ECF/ECD para MEI já estava correta.

## 0-AJ. [RESOLVIDO NESTA RODADA, 11/09/2026 parte 15, pedido do usuário com print real: "mesmo colocando o CCMEI, ele está pedindo revisão [...] tem que saber ler todo o documento"] CCMEI anexado ficava permanentemente preso em "Revisão necessária — Campos essenciais não comprovados: titular, condicao_mei", mesmo sendo um documento real e legível

O CCMEI nunca teve um leitor local especializado -- caía no `parseDocumentoGenerico`, cujo teto de confiança (0.65) nunca alcança o limiar (0.72) que permite aceitar a leitura local como definitiva; os rótulos reais do CCMEI ("Nome Civil", cabeçalho às vezes quebrado em duas linhas) não eram reconhecidos pelo parser genérico. Corrigido com `parseCcmei` (`extracaoDocumentalLocal.ts`), seguindo o mesmo padrão dos parsers especializados já existentes (DASN-SIMEI, DEFIS): extração dedicada de titular/condição de MEI/data de início/situação cadastral, com confiança elevada o suficiente para cruzar o limiar quando o documento é reconhecido. Roteado em `tipoLeitorLocalDocumentoCatalogado` e no dispatcher `analisarTextoDocumentoLocal`. Ver `tests/parseCcmei.test.ts` (8 testes). Nota fora do escopo de código: as variáveis de ambiente `DOCUMENT_EXTERNAL_AI_FALLBACK_ENABLED`/`GEMINI_DOCUMENT_OCR_ENABLED` (fallback de IA externa) valem a pena confirmar no Coolify para a qualidade geral de leitura de documentos, embora não sejam necessárias para esta correção específica.

## 0-AI. [RESOLVIDO NESTA RODADA, 10/09/2026 parte 14, CAUSA RAIZ REAL -- usuário interrompeu a rodada anterior com 2 prints novos: "eu não consegui ainda localizar onde vai o CCMEI. Isso é, saia desse loop, saia desse erro, e resolva, de uma vez"] Campo dedicado "CCMEI" nunca aparecia na grade, para NENHUMA empresa, em nenhum regime -- faltava nas listas `tiposPermitidos` das duas telas de Acervo Documental de empresa, não em nenhuma regra de MEI das partes 11/12

A grade de campos não vem direto do catálogo estático (`SECOES_DOCUMENTAIS`) -- passa antes por um filtro de "tipos permitidos" (`tiposPermitidos`, prop de cada tela) dentro de `slotsDaTela` em `DocumentosEntidade.tsx`: um campo só entra na grade se o tipo estiver nessa lista OU já tiver arquivo anexado. "ccmei", "das_mei" e "relatorio_receitas_mei" têm slot cadastrado no catálogo mas nunca estiveram em `TIPOS_EMPRESA` (`AcervoDocumentalEmpresa.tsx`) nem em `TIPOS_PERMITIDOS_EMPRESA` (`EmpresaDocumentos.tsx`) -- confirmado por busca no código-fonte inteiro (`client/src/pages`), a palavra "ccmei" não aparecia em nenhum lugar. Essa é a causa raiz real de toda a confusão relatada desde a parte 11: nenhuma correção de MEI (reordenar, esconder, textos de mensagem) podia ter efeito sobre um campo que nunca chegava a existir na grade em primeiro lugar. Efeito em cascata sobre "Atos da Junta"/"Contrato social" continuarem pendentes: o backend só dispensa esses dois campos para MEI quando o CCMEI foi de fato anexado e reconhecido (`societaria.atos_dispensados_por_mei`, correto desde a parte 7/8/9) -- sem um campo para anexar o CCMEI, essa evidência nunca podia existir, e o bloqueio "anexe o CCMEI para liberar" (já implementado desde a parte 9) instruía uma ação que o usuário não conseguia realizar. Corrigido adicionando os três tipos às duas listas. Investigado e descartado usar o padrão "empresário individual" (de `isEmpresaIndividual`) para inferir o MEI sem exigir o CCMEI -- causaria a mesma regressão que a Rodada 29 já corrigiu (nem todo Empresário Individual é MEI). Ver `tests/tiposPermitidosEmpresaIncluiCcmei.test.ts` (4 testes).

## 0-AH. [RESOLVIDO NESTA RODADA, 10/09/2026 parte 13 -- pedido do usuário sobre datas: "temos que ter isso tudo de acordo com as datas... [a DEFIS] de uma empresa que desenquadrou agora, só vai sair no meio do ano que vem"] ECF/ECD/DEFIS/DASN-SIMEI viravam pendência obrigatória mesmo antes do prazo legal de entrega, para empresa que mudou de regime recentemente

As calculadoras de prazo legal (ECF: último dia útil de julho do ano seguinte; ECD: último dia útil de junho; DEFIS: 31/03; DASN-SIMEI: 31/05) já existiam desde a Rodada 33, mas só eram usadas para julgar documento já anexado -- nunca para decidir se um documento ainda faltando devia contar como pendência obrigatória. Corrigido com `aplicarExigibilidadeTemporalDocumentosAnuais` (`mapaDocumentalCreditoService.ts`), ligada em `documentacao.ts` logo após o cálculo da linha do tempo de regime: enquanto o prazo legal não chega, o documento deixa de contar como obrigatório (`obrigatorio: false`, `status: 'ainda_nao_exigivel'`). Ver `tests/exigibilidadeTemporalDocumentosAnuais.test.ts` (7 testes).

## 0-AG. [RESOLVIDO NESTA RODADA, 10/09/2026 parte 12 -- usuário revendo a própria correção da parte 11 com print real: "não precisa aparecer atos à junta nem o contrato social... só vai aparecer os campos necessários"] Para MEI, "Atos da Junta Comercial" e "Contrato social" agora somem da grade (CCMEI vira o primeiro campo) -- reversível automaticamente se a empresa desenquadrar de MEI

A parte 11 (mais cedo no mesmo dia) só reordenava o CCMEI para perto de Contrato Social, sem esconder nada. O usuário pediu mais: que os dois campos que o MEI nunca usa (Atos da Junta, Contrato Social) simplesmente não apareçam na grade, e que o CCMEI vire o primeiro campo -- com a condição explícita de que isso reverta sozinho se a empresa um dia desenquadrar do MEI. Implementado com uma exceção de segurança (zero regressão): um campo só é escondido se não tiver nenhum arquivo já anexado -- um upload já existente (como o CCMEI anexado por engano em "Contrato social" no print do usuário) nunca fica escondido/inacessível. A reversibilidade é automática: `empresa_identificada_mei` é recalculado a cada carregamento a partir do enquadramento tributário real (nunca hardcoded), então se a empresa mudar de regime, os campos voltam sozinhos. Também aumentado o contraste do título de cada card (mesmo peso, cor mais escura), a pedido do usuário. Ver `tests/reordenacaoCcmeiParaMei.test.ts` (6 testes).

## 0-AF. [RESOLVIDO, 10/09/2026 parte 11, SUBSTITUÍDO PELA PARTE 12 ACIMA -- usuário perguntou "onde anexo o CCMEI, pois estou anexando no mesmo local do contrato social"] Campo dedicado "CCMEI" ficava 24 campos abaixo de "Contrato social" na lista, sem nenhuma ordenação por MEI -- causa real da confusão sobre onde anexar

O campo dedicado "CCMEI" sempre existiu na mesma aba/seção de "Contrato social" e "Atos da Junta Comercial" (nunca em outra tela) -- mas era o 18º item de uma lista de ~40 campos, 24 posições abaixo de "Contrato social". A ordem de renderização sempre seguiu a ordem de declaração no array-fonte, sem nenhum critério de prioridade ou MEI. Anexar em "Contrato social" já funciona (reconhecido pelo classificador desde a parte 8), mas o usuário nunca via o campo dedicado sem rolar a tela inteira -- por isso a pergunta "vai ter outro local pra mim anexar, ou eu vou anexar no local do contrato social mesmo?". Corrigido com uma reordenação condicional (só para `empresa_identificada_mei === true`): o campo "CCMEI" passa a aparecer logo depois de "Contrato social" na grade. Nenhum campo foi escondido, removido ou desabilitado -- Atos da Junta e Contrato Social continuam visíveis (já marcados "Dispensado (MEI)" desde a parte 10), atendendo ao pedido de "não precisar" delas sem de fato tirá-las da tela. A descrição do campo CCMEI também ganhou a referência cruzada reversa (antes só os outros dois campos mencionavam o CCMEI). Ver `tests/reordenacaoCcmeiParaMei.test.ts` (3 testes).

## 0-AE. [RESOLVIDO NESTA RODADA, 09/09/2026 parte 10 -- usuário insistiu que mesmo após o redeploy do v44 a etapa ainda pedia "atos da junta"] Três textos de tela/mensagem ainda diziam "Atos da Junta Comercial" para MEI, mesmo com a gate de negócio (parte 9) já correta

A gate de negócio (`atos_dispensados_por_mei`) já estava correta desde a parte 9. O que faltava: enquanto a etapa não avança (CCMEI ainda pendente ou não reconhecido), TRÊS textos continuavam dizendo "Atos da Junta Comercial" para qualquer empresa, inclusive MEI, sem nunca checar o tipo de empresa: (1) "próximo documento a anexar" + título do card + rótulo do botão, na Etapa 2/3 do Acervo Documental (`DocumentosEntidade.tsx`); (2) aviso de "ordem recomendada" no campo Contrato Social (mesmo arquivo); (3) mensagem de erro da rota `POST /empresa/:empresaId/analise-societaria/iniciar` (`server/routes/documentacao.ts`). Corrigido com um novo campo explícito e aditivo, `empresa_identificada_mei` (retornado por `montarValidacaoSocietaria`, verdadeiro assim que a empresa é reconhecida como MEI -- independente de o CCMEI já ter sido anexado ou não) -- as três superfícies agora ramificam por ele e nunca mais mencionam "Atos da Junta" para MEI. Ver `tests/textoMeiSemAtosDaJuntaUiCompleta.test.ts` (6 testes) e `tests/validacaoSocietariaCcmeiMei.test.ts` (assertions novas em todos os 5 cenários, incluindo a confirmação de que uma LTDA continua com `empresa_identificada_mei: false`).

## 0-AD. [RESOLVIDO NESTA RODADA, 09/09/2026 parte 9 -- novo print real mostrando que a etapa ainda pedia o CCMEI mesmo depois da parte 8] Caminho de leitura do tipo detectado estava errado em `montarValidacaoSocietaria`

A correção da parte 8 (classificador aceitando CCMEI no Contrato Social) estava certa, mas o código que lê essa informação em `montarValidacaoSocietaria` checava `analise_regra_documental.tipo_detectado` -- um caminho que nunca existe de verdade. O valor real fica em `analise_regra_documental.dados_extraidos.tipo_detectado` (confirmado contra o formato já usado, de forma independente, em `tests/documentacaoConclusaoIncompatibilidade.test.ts`). Corrigido o caminho de leitura; o teste que cobre este cenário também foi corrigido (usava um formato simplificado que não reproduzia o formato real, por isso não pegou o bug).

## 0-AC. [RESOLVIDO NESTA RODADA, 09/09/2026 parte 8 -- print real mostrando o card "Documento incompatível"] CCMEI anexado no campo "Contrato social" era rejeitado, e a etapa não reconhecia essa evidência

Print real: empresa MEI (CNPJ 29.705.345/0001-22), usuário anexou o CCMEI no campo "Contrato social e alterações contratuais" -- exatamente como pediu na rodada anterior. Dois problemas: (1) o card mostrava "Documento incompatível" -- `classificadorDocumentalCentral.ts` não aceitava CCMEI como tipo detectado compatível com o campo Contrato Social; corrigido (CCMEI é exclusivo do MEI, nunca existe para LTDA, então aceitar não abre brecha). (2) mesmo sem o "incompatível", a etapa não avançava -- `montarValidacaoSocietaria` só procurava CCMEI por `tipo_documento = 'ccmei'` (o campo dedicado), não pelo tipo realmente detectado dentro do campo Contrato Social; corrigido para também checar `resultado_validacao.analise_regra_documental.tipo_detectado === 'CCMEI'` nos documentos anexados como contrato social/alteração contratual.

## 0-AB. [RESOLVIDO NESTA RODADA, 09/09/2026 parte 7] Etapa 2 (societária) não reconhecia o CCMEI como equivalente do Contrato Social/Atos da Junta para MEI

Pedido do usuário: "o mei tambem como se fosse o contrato social que o CCmei, la esta o nome e dados do socio, então não tem atos da junta mas quando mei em contrato social coloca o ccmei, e avança". MEI não tem Atos da Junta/Contrato Social -- o documento equivalente é o CCMEI. Corrigido em `montarValidacaoSocietaria` (`server/routes/documentacao.ts`): (1) a detecção local de MEI desta etapa era mais estreita que `isEmpresaIndividual` e não reconhecia "Empresário (Individual)" com parênteses -- unificada com a detecção canônica; (2) a dispensa dos Atos da Junta/Contrato Social, antes em branco (sem exigir nada), agora só se confirma com o CCMEI efetivamente anexado e com conteúdo legível -- sem o CCMEI, a etapa fica pendente com um bloqueio orientando a anexá-lo, em vez de avançar sozinha. Ver `tests/validacaoSocietariaCcmeiMei.test.ts` (4 testes).

## 0-AA. [RESOLVIDO NESTA RODADA, 09/09/2026 parte 6 -- a causa raiz real só foi encontrada com os 3 documentos reais que o usuário anexou] MEI/EI sem sócio no QSA travava 100% da documentação pessoal -- titular agora é criado automaticamente, e a empresa agora É reconhecida como Empresário Individual/MEI

O bug relatado (empresa MEI real "55.497.701 NATALYA MARTINS LOBO" com "Documentos dos sócios" em 0/14, todos os 14 campos bloqueados com "Sincronize o QSA para identificar o sócio antes de anexar") está corrigido de ponta a ponta -- confirmado com os 3 documentos reais da empresa (Cartão CNPJ, Consulta Optantes/SIMEI, QSA) que o usuário anexou. **Histórico do que aconteceu, importante para quem for verificar:**
- Parte 4: criou `garantirTitularEmpresaIndividual` (`server/routes/documentacao.ts`) -- cria automaticamente uma linha "Titular" em `socios_empresa` para EI/MEI sem sócio -- mas só ligada dentro do dossiê (`montarDossieCreditoEmpresa`).
- Parte 5: a tela de Acervo Documental lê os sócios de uma rota SEPARADA (`GET /api/empresas/:id/socios`), então o bloqueio continuava -- corrigido ligando a mesma função também nessa rota. Também adicionada a extração do nome a partir do nome empresarial do MEI.
- Parte 6 (causa raiz real): mesmo com as duas rotas corrigidas, o bug persistia -- porque `isEmpresaIndividual` (a função que decide se a empresa é EI/MEI, e da qual toda a correção depende) usava uma regex que exigia "empresario individual" com as duas palavras coladas. O texto oficial real da Receita Federal para essa natureza jurídica (confirmado no Cartão CNPJ anexado) é **"213-5 - Empresário (Individual)"**, com parênteses -- a regex nunca batia, e a função retornava `false` para essa empresa real (o campo `opcao_mei` também não estava sincronizado no cadastro). Corrigido para aceitar o parêntese.

Nada disso inventa CPF, cria tabela nova ou duplica registro em chamadas repetidas. Ver `CHANGELOG_CORRECOES.md` (Rodada 09/09/2026 partes 4, 5 e 6) para a descrição completa e `tests/garantirTitularEmpresaIndividual.test.ts` (18 testes) + `tests/getSociosEmpresaRotaReconciliaTitular.test.ts` (4 testes) + `tests/isEmpresaIndividualSemNomeDaEmpresa.test.ts` (9 testes) para a cobertura.

## 0-AB. O pedido original (53 seções) pedia um sistema completo de papéis jurídicos/histórico societário/documentação declarativa por natureza jurídica -- só o travamento real e demonstrado (MEI/EI sem QSA) foi resolvido nesta rodada; o resto falta

O pedido trazia, além do bug do titular (já resolvido, ver 0-AA), uma arquitetura bem mais ampla que NÃO foi construída nesta rodada -- por decisão deliberada, seguindo a instrução do próprio pedido de preferir "evoluir a arquitetura existente com o menor impacto seguro" e "não criar uma segunda arquitetura paralela" em vez de reescrever tudo de uma vez com alto risco de regressão. Falta, especificamente:
- Papéis jurídicos além de TITULAR (SOCIO_ADMINISTRADOR, ADMINISTRADOR, DIRETOR, PRESIDENTE, REPRESENTANTE) -- hoje só existe TITULAR (MEI/EI) e SOCIO (todo o resto, sem diferenciação de papel dentro do grupo).
- `CompanyHistoricalState`/linha do tempo de transformação societária (MEI→LTDA, EI→LTDA, etc.) -- hoje o sistema só olha o estado atual da empresa, não preserva nem resolve documentação histórica por janela de análise.
- Resolvedor declarativo único de documentação por natureza jurídica (`DocumentRequirementSpec`) cobrindo explicitamente LTDA, SLU, SA, Cooperativa, Associação/Fundação e Sociedade de Advocacia -- hoje cada uma dessas naturezas já tem seu próprio tratamento espalhado pelo código (funcionando, testado, sem regressão), mas não unificado numa única estrutura declarativa.
- Engine de cross-check entre CNPJ/QSA/CCMEI/Enquadramento/Atos da Junta/Contrato Social gerando evidências e nível de confiança -- hoje cada fonte é lida e comparada isoladamente, sem um "cross-check" central que produza uma confiança consolidada por pessoa/natureza jurídica.
- Estados de identidade explícitos (`CONFIRMED`/`PROBABLE`/`PENDING_DOCUMENTATION`/`CONFLICT`) expostos ao frontend -- hoje o titular criado sem nome/CPF confirmado já libera o upload (o comportamento certo), mas não existe um rótulo de status de identidade visível na tela para o usuário do Destrava.
- Mensagens de UI específicas para MEI ("Titular identificado"/"Complete a documentação do titular" em vez de linguagem de "sócio") -- não foi alterado; hoje a tela simplesmente não mostra mais o aviso de bloqueio (porque o titular existe e o upload já funciona), mas o rótulo do seletor "Documento de quem?" ainda usa o nome do titular como está gravado (inclusive o placeholder "Titular da empresa (nome a confirmar)" quando não há nome resolvido), sem um selo textual dedicado do tipo "Titular identificado".

## 0-Z. [ACHADO IMPORTANTE] O "cérebro único" pedido pelo usuário (09/09/2026) já existe no repositório -- construído, testado, ligado de ponta a ponta -- só não foi verificado nesta rodada se está entregando o que o usuário espera dele

Investigando o pedido de "cérebro único" (ver item 0-Y abaixo), encontrei que `server/services/inteligencia360Service.ts` (1400 linhas) + `server/services/indicadoresFinanceiros.ts` (336 linhas) já implementam quase exatamente o que foi pedido: um motor determinístico único que já recebe acompanhamento bancário (rating, evolução), faturamento rolling 12 meses, cobertura de evidência entre bureaus, documentos, sócios, simulações, contratos, histórico e followups -- e já calcula score (`score_destrava`), risco de crédito, saúde cadastral/documental, prontidão para contrato/proposta bancária, elegibilidade de crédito por programa (crédito bancário padrão, PRONAMPE, BNDES indireto), plano de adequação, pendências, recomendações priorizadas, diagnóstico geral e "caminho sugerido"/"próximas ações". **Já está ligado de ponta a ponta**: rota `GET /api/empresas/:id/inteligencia-360` (`server/index.ts`, linha ~6814) chama `calcularInteligencia360(...)`, e o frontend já tem uma aba própria "Inteligência 360" na página de cada empresa (`client/src/pages/colaborador/Inteligencia360.tsx`, mais os blocos satélites `PlanoAcaoMotor.tsx`, `RelatorioTecnico.tsx`, `PropostaBancaria.tsx`, `Historico360.tsx`), já habilitada por padrão (feature flag `empresa-tab-inteligencia-360`, que fica ligada a menos que um administrador desligue explicitamente). Também já tem 71 testes cobrindo o serviço (`tests/inteligencia360.test.ts`, `tests/inteligencia360Documental.test.ts`, `tests/catalogoRegrasFinanceiro.test.ts`).

**O que isso muda:** construir um "cérebro" do zero agora seria criar exatamente a segunda arquitetura paralela que o usuário já proibiu explicitamente numa rodada anterior ("não crie uma segunda arquitetura paralela"). Não fiz isso. O que não foi feito nesta rodada, por falta de tempo/orientação do usuário: (1) abrir a aba "Inteligência 360" numa empresa real e conferir se o que ela mostra hoje já corresponde ao que o usuário quis dizer com "vê tudo, enxerga lacunas, oportunidades e pontos críticos"; (2) verificar se falta algo específico dos três pilares citados pelo usuário (acompanhamento bancário/faturamento/elegibilidade) no que já está calculado; (3) o autoaprendizado diário e a "conversa com o time comercial e de análise" (interface conversacional) -- isso, sim, não existe hoje em lugar nenhum do código, é trabalho genuinamente novo. Ver a resposta desta rodada para a recomendação de próximo passo.

## 0-Y. Pedido de "cérebro único" de IA (comercial + análise + estratégia, autoaprendizado diário) -- parcialmente já construído (ver 0-Z); falta confirmar com o usuário e endereçar o que é genuinamente novo (autoaprendizado, interface conversacional)

O usuário pediu (09/09/2026) um sistema que "vê tudo, enxerga lacunas, oportunidades e pontos críticos", une acompanhamento bancário + faturamento de 12 meses + elegibilidade de crédito num "só cérebro", fica mais inteligente todo dia, e conversa com o time comercial e de análise. A parte de "ver tudo/enxergar lacunas/elegibilidade" já existe e está ligada em produção (ver item 0-Z) -- o que falta claramente, sem ambiguidade, é: (a) confirmar com o usuário se a aba "Inteligência 360" já existente resolve a intenção, ou se falta algo específico nela; (b) autoaprendizado diário (nada no código hoje reavalia/reaprende sozinho -- o motor é determinístico, recalcula na hora que a tela é aberta, não "aprende" com o tempo); (c) uma interface conversacional (chat) que converse com o time comercial/análise -- não existe hoje.

## 0-X. [RESOLVIDO] Cards do Acervo Documental agora começam ocultos por padrão, só título + selo de resultado + ícone i

Implementado na mesma rodada (09/09/2026, parte 3) -- ver `CHANGELOG_CORRECOES.md`, seção "Rodada 09/09/2026 (parte 3)". Cada card de tipo de documento agora começa recolhido, mostrando só título, selo de resultado (Validado/Pendente/Não anexado/Incompatível/Revisão necessária/Dispensado/Coberto) e o ícone "i" -- o resto (Anexar, observação, StatusAnaliseSlot, lista de arquivos) fica atrás de um toggle (seta).

## 0-W. Ícone "i" de descrição do campo e "+N arquivos -- ver todos" continuam abrindo por clique, não por hover

Só o painel "Dados da análise"/laudo (por documento) virou hover nesta rodada (09/09/2026). O ícone "i" que explica o que é cada tipo de documento, e o botão "+N arquivos -- ver todos", continuam abrindo por clique. Não é bug -- são conteúdos curtos ou que já rolam dentro da própria caixa, não geram cards de tamanho diferente. Se o usuário quiser os dois também em hover, é só pedir.

## 0-V. Ícone de hover ("Dados da análise") ainda não foi testado em celular/tablet (touch, sem mouse)

O novo ícone abre ao passar o mouse. Em touch pode não abrir com um toque só (comportamento do navegador, não testado aqui). Se isso acontecer, o ajuste é trocar para "toque abre, toque fora fecha" -- avisar se for o caso.

## 0-U. [RESOLVIDO] Terceiro documento real (o par completo Atos da Junta + Alteração Contratual de VIK CONSTRUÇÕES E REFORMAS LTDA) revelou 2 bugs adicionais de extração no Contrato Social/Alteração -- e a investigação do "print bagunçado" não encontrou bug de layout

O usuário reportou (09/09/2026), sobre a tela de produção: **"ele não conseguiu validar o atos da junta e alteração no contrato social, mas está tudo certo, os dados estão todos corretos nos dois documentos"**, junto com um print da tela do Acervo Documental e a queixa **"ficou totalmente desformatada, bagunçada, desorganizada... modais maiores que os outros, sem a opção de ocultar... com leituras... sem precisão"**. Dois documentos reais adicionais foram anexados: `alteraA_A_o_Vik_construA_A_es_1.pdf` (a Alteração Contratual completa, real e assinada, de VIK CONSTRUÇÕES E REFORMAS LTDA) e `atos_da_junta_Vik_1.pdf` (idêntico ao `atos_da_junta_Vik.pdf` já usado na atualização anterior) -- pela primeira vez o par real e completo dos dois lados confrontados pela Etapa 2, da mesma empresa.

**Validação (backend):** rodando os dois documentos reais juntos, a identificação por evidência (correção original de 08/09/2026) funcionou corretamente nos dois lados desde o primeiro momento (`documento_compativel: true` para os dois). Dois problemas adicionais de EXTRAÇÃO (não de identidade) foram encontrados e corrigidos -- ver `CHANGELOG_CORRECOES.md`, subseção "Segunda atualização da mesma rodada (09/09/2026)": (1) `numero_arquivamento` não reconhecia a ordem "Certifico registro sob o nº X em DATA" (JCDF, ordem invertida em relação ao padrão de Goiás já coberto); (2) a extração de sócios fabricava um "sócio" com o nome literal "administrador" a partir da linha de assinatura "Sócio - administrador" (bug de regex, flag `/i` aplicada também à captura do nome). Depois da correção, o par real completo valida sem nenhum alerta de incompatibilidade ou divergência (novo teste, `tests/atosJuntaIdentidadePorEvidencia.test.ts`, terceiro `describe`).

**Investigação do "print bagunçado" (frontend) -- nenhum bug de código encontrado.** Confirmado por leitura direta do código: o toggle "ocultar"/"Dados da análise" por documento já existe e funciona (`DocumentosEntidade.tsx`, ~linha 2528) -- é o mesmo link visível no print do usuário. O card ficar mais alto que os vizinhos da mesma linha quando o painel de validação está aberto é o comportamento intencional e já documentado da Rodada 30 (`align-items: stretch` do CSS Grid -- quando um card cresce de verdade, a linha inteira cresce, e os vizinhos mais curtos esticam a própria caixa para acompanhar, em vez de sobrar vão em branco); nenhuma classe CSS ou componente de layout foi tocado desde a Rodada 30. **Hipótese mais provável para a queixa, não comprovada por print adicional:** no momento do print, o card provavelmente mostrava o estado "Documento incompatível" (efeito dos dois bugs de backend acima, ainda não publicados em produção) em vez do painel de confirmação normal -- um alerta de erro tende a parecer mais "bagunçado"/"impreciso" que uma confirmação limpa, mesmo sem nenhuma mudança real de CSS. Fica registrado aqui, honestamente, para confirmação: se depois desta correção ser publicada e os dois documentos passarem a validar, o card ainda parecer "torto" ou sem a opção de ocultar em algum caso específico, um novo print real (com os documentos já validados) permite uma investigação direta e cirúrgica desse ponto especificamente -- não foi feita nenhuma alteração de frontend nesta rodada por falta de uma causa raiz comprovada de código, seguindo a mesma regra de todas as rodadas anteriores ("não altere código sem causa raiz confirmada por leitura direta").

**Risco correlato identificado, mas deliberadamente NÃO corrigido nesta rodada:** o mesmo padrão de bug do item (2) acima (`new RegExp(padrao, 'i')` aplicado também à captura de um nome que deveria exigir maiúsculas) existe em `parseContratoSocialAlteracao`, na extração de `cedente`/`cessionario` de transferência de cotas -- não comprovado quebrado por nenhum documento real até agora (a fixture de teste existente já usa nomes genuinamente maiúsculos). Registrado aqui para correção no mesmo padrão, caso um documento real algum dia exponha o mesmo sintoma nesse campo específico.

**Limite honesto, não um bug:** o nome do sócio-administrador do documento real (IVANILDO FERREIRA DS ANJOS) aparece numa frase corrida do corpo do texto, longe de qualquer rótulo "Sócio:"/"Administrador:" adjacente -- os extratores atuais não alcançam esse padrão; o resultado correto após a correção é `socios: []` (nada inventado). Extrair esse nome exigiria um extrator mais amplo (busca por nome próprio em frase corrida), fora do escopo desta correção cirúrgica -- disponível para uma rodada futura, mediante confirmação do usuário.

## 0-T. [RESOLVIDO] Correção de 08/09/2026 foi validada com um documento fictício primeiro; os dois PDFs reais foram anexados em seguida e confirmaram a correção -- e revelaram 2 bugs adicionais reais, também corrigidos

A missão descrevia o envio de dois documentos reais (um reconhecido corretamente, outro rejeitado). Eles não vieram junto com o zip do repositório, então a causa raiz foi localizada por leitura direta do código e a correção foi validada primeiro com um documento sintético equivalente. Os dois documentos reais (`CertidA_o_Online__Portal_do_Empreendedor_Goiano_3.pdf` -- Lista de Arquivamentos, PALUMA BURGER LTDA, GO; `atos_da_junta_Vik.pdf` -- tela "Atos disponíveis", sem CNPJ/NIRE/razão social visíveis, empresa transformada de EIRELI em LTDA) foram anexados na mensagem seguinte e usados para confirmar a correção com o texto extraído exato de cada um (ver `tests/atosJuntaIdentidadePorEvidencia.test.ts`, describe "reprodução com os dois documentos reais da missão"). O documento real "Vik" confirmou o diagnóstico (passou a ser reconhecido) e revelou 2 problemas adicionais de EXTRAÇÃO (não de identidade) que o documento sintético não cobria, ambos corrigidos na mesma rodada:

1. **NIRE não inferido do Ato Constitutivo:** o documento real usa "ATO CONSTITUTIVO", não "CONSTITUIÇÃO"/"CONSTITUINTE" -- a lista de palavras-chave que reconhece atos de registro (tanto para montar o histórico quanto para inferir o NIRE quando não há rótulo explícito) só cobria o radical "constituic". Ampliado para o radical mais genérico "constitu" (cobre "constituição", "constitutivo", "constituinte" -- vocabulário universal de registro mercantil, não específico de nenhuma Junta).
2. **Razão social inventada a partir do título de um ato:** o fallback que procura uma "linha solta contendo LTDA/EIRELI" (usado quando não há rótulo explícito de nome empresarial) confundia o cabeçalho de um cartão de ato ("ATO CONSTITUTIVO - EIRELI") com o nome da empresa, só porque a palavra "EIRELI" aparece nos dois contextos. Corrigido excluindo linhas que são, elas mesmas, cabeçalhos de tipo de ato/evento dessa busca -- o documento real não tem nenhum campo de nome empresarial, e agora `razao_social` corretamente vem `null` em vez de um valor errado.

Os 4 arquivamentos do documento real "Vik" agora são extraídos com o tipo de ato correto para cada um (sem contaminação entre blocos vizinhos), e o NIRE (53600026039) é corretamente inferido do Ato Constitutivo -- EIRELI. O Documento A real (PALUMA BURGER LTDA) permanece extraído de forma idêntica a antes da correção (prova de não-regressão com o documento real, não só com fixtures).

## 0-S. Identidade por evidência (08/09/2026) cobriu só Atos da Junta e Contrato Social/Alterações -- os outros ~13 tipos de documento local ainda decidem `documento_compativel` só pela frase literal

`server/services/extracaoDocumentalLocal.ts` tem o mesmo padrão -- `documento_compativel` decidido só por um OR de frases/palavras literais, ignorando o score de confiança já calculado -- em outras funções de parser além de `parseAtosJunta`/`parseContratoSocialAlteracao` (ex.: QSA, Cartão CNPJ, extrato bancário, comprovante de regime, faturamento 12 meses, EFD-Contribuições). A missão que motivou a correção de 08/09/2026 era explicitamente escopada a Atos da Junta Comercial (e ao par que ele confronta, Contrato Social), com instrução explícita de não fazer refatoração indiscriminada nem tocar em código fora do necessário -- por isso os outros parsers não foram alterados. Se o usuário observar o mesmo sintoma ("documento genuíno, mas marcado incompatível por causa de vocabulário/layout diferente") em outro tipo de documento, o mesmo padrão de correção (evidência extraída substituindo/complementando a frase literal como sinal de compatibilidade, com o cuidado de não introduzir falso positivo) pode ser aplicado da mesma forma, tipo por tipo, mediante confirmação -- não é automático para os demais tipos só por esta rodada.

## 0-R2. Duas outras linhas de desenvolvimento (outras sessões de IA) tocaram este mesmo repositório entre a Rodada 36 anterior e esta entrega -- não foram totalmente auditadas nesta rodada

O zip usado como base desta rodada (`destravamain_30.zip`) já veio com trabalho de pelo menos duas outras sessões: uma branch "Rodada 36 -- validação documental persistente e internal-first" (`RELATORIO_RODADA_36_INTELIGENCIA_DOCUMENTAL_INTERNA_2026-09-05.md`) e uma branch "recuperação estável" baseada num commit mais antigo (`RELATORIO_VALIDACAO_DOCUMENTAL_RECUPERACAO_2026-09-05.md`), que por sua vez afirma que "os 141 tipos catalogados possuem ficha de leitura interna explícita". Essa afirmação foi checada nesta rodada e está **superestimada**: o que existe de fato em código (`server/services/documentAnalysisProfiles.ts`, tabela `POLITICA_POR_TIPO`) é uma política de temporalidade/validade por tipo (`grauFonte`, prazo de validade), não uma "ficha" completa com campos obrigatórios/opcionais, sinais de identidade, validadores e cruzamentos por tipo (o que a missão de 08/09/2026 chama de `DocumentReaderSpec`). Registrado aqui com honestidade, como pedido -- não foi construída nesta rodada uma segunda arquitetura paralela para isso; a correção de 08/09/2026 evoluiu a função de parser existente, no lugar onde o bug real estava. Também não foi reconciliado nesta rodada o valor de `MIGRATE_ON_STARTUP` (esta cópia do repositório está com `false`, valor defendido pela branch de "recuperação"; uma rodada anterior minha -- fora desta sessão de zip -- havia mudado esse valor para `true` num repositório diferente/anterior). Este arquivo (`MIGRATE_ON_STARTUP`) não foi tocado nesta rodada por não ter relação com a missão de identidade documental e por já estar em um valor que a outra sessão documentou como decisão deliberada -- fica registrado aqui só para o usuário decidir, com conhecimento do estado real, qual das duas preferências deve prevalecer.

## 0-R. Nivelamento dos cards (Rodada 30) é sobre a CAIXA visual (borda/fundo) de cada card -- não reduz nem uniformiza o CONTEÚDO de cada tipo de campo

A Rodada 30 corrigiu só a forma como cada card PREENCHE o espaço vertical dentro da grade (a caixa estica até a altura da linha, em vez de flutuar com um vão em branco por baixo). Ela não torna o CONTEÚDO de cada card idêntico -- um card com selo "OBRIGATÓRIO NA ETAPA" continua tendo mais texto no topo do que um sem selo, e um card com "Dados da análise" disponível continua tendo esse link, enquanto outro tipo de documento sem leitura automática (ex.: Relatório SCR/Registro) nunca tem esse link. Essa diferença de CONTEÚDO entre tipos de documento é esperada e correta (cada tipo de documento realmente tem informações diferentes para mostrar) -- o que a Rodada 30 elimina é só a diferença de ALTURA DA CAIXA que sobrava daí, não a diferença de conteúdo em si.

## 0-Q. Auditoria da Rodada 29 não foi uma varredura exaustiva de toda combinação regime × porte × natureza jurídica -- é uma auditoria dirigida, não uma prova formal de que não existe mais nenhuma inconsistência

A Rodada 29 foi motivada por um pedido genérico do usuário (sem print, reforçando a regra geral já em vigor desde o início do projeto: "visual/modais idênticos entre tipos de empresa, só o dado muda"), não por um bug específico relatado. A investigação cobriu profundamente a lógica central que decide o que aparece na tela e o que é diagnosticado por tipo de empresa (`shared/documentalPresentation.ts`, `mapaDocumentalCreditoService.ts`, `documentacao.ts`, `analiseDocumentalEspecializada.ts`, `analiseCnpjReceitaCartao.ts`, `DocumentosEntidade.tsx`, `DossieCreditoEmpresa.tsx`) e encontrou três inconsistências reais, todas corrigidas (ver `CHANGELOG_CORRECOES.md`, Rodada 29). Não é, porém, uma prova formal de que não existe mais nenhuma diferença estrutural indevida em algum canto do sistema não coberto por essa lista de arquivos -- o projeto não tem infraestrutura de teste de componente React para varrer automaticamente todas as combinações possíveis de regime/porte/natureza jurídica contra todas as telas. Fica registrado aqui, honestamente, para o caso de o usuário observar no futuro alguma outra tela/modal com aparência diferente entre dois tipos de empresa -- o próximo passo continua sendo o mesmo de sempre: um print real permite uma investigação cirúrgica direta.

## 0-P. Ordem de anexo (Contrato Social antes do Ato da Junta) continua podendo deixar um card preso em "Aguardando análise" até o segundo documento ser anexado -- comportamento esperado, não alterado nesta rodada

O caso concreto do print da Rodada 28 (Contrato Social de "OFICINA DA BELEZA LTDA" anexado em 31/08 15:09, Ato da Junta só em 01/09 11:12) mostrou o Contrato Social preso em "Aguardando análise" até o botão "Reler" (corrigido nesta rodada) ser clicado manualmente. Isso é esperado: a análise de Contrato Social sempre precisa do Ato da Junta para confrontar contra ele (`analisarContratoComAtosJunta`), então, se o Contrato Social é anexado primeiro, a tentativa automática do momento do upload não tem contra o que comparar -- ela só se resolve sozinha quando o Ato da Junta chega depois (o gatilho `TIPOS_GATILHO_ANALISE_SOCIETARIA` já cobre os dois sentidos) ou, agora, com um clique manual em "Reler". Não foi alterado nesta rodada porque não é um bug -- é uma dependência real entre os dois documentos, válida para qualquer empresa. Se o usuário quiser, uma melhoria futura poderia disparar automaticamente uma nova tentativa de TODOS os documentos societários pendentes assim que qualquer um dos dois chegar (hoje o gatilho só reanalisa o par recém-completo, não varre pendências antigas de uploads anteriores) -- fica registrado aqui para confirmação explícita antes de ser implementado.

## 0-O. Situação cadastral (Rodada 20) ainda corrige só na PRÓXIMA análise -- nome empresarial (Rodada 26) já corrige na MESMA análise; a assimetria é intencional por ora, pode ser unificada mediante confirmação

A Rodada 26 posicionou a correção de nome empresarial ANTES do cálculo de `comparacao`/`divergencias` dentro de `analisarCnpjReceitaCartaoEmpresa`, para que o resultado já apareça certo "no modal a análise" -- ver `CHANGELOG_CORRECOES.md`, seção "Rodada 26", exatamente como o usuário pediu para este campo. A confirmação de situação cadastral (Rodada 20) continua rodando DEPOIS, como efeito colateral do bloco `if (persistir)` mais abaixo -- então, se uma correção de situação cadastral for aplicada nesta mesma leitura, o card "Revisão necessária" desta mesma passada ainda pode mostrar uma divergência de situação já tecnicamente corrigida no banco, só refletindo o valor certo na próxima análise (novo upload, nova retentativa de 15 min, ou F5). Isso não é uma regressão desta rodada -- é o comportamento que já existia desde a Rodada 20, só ficou mais visível pelo contraste com a nova correção de nome. Não foi alterado agora para manter o escopo cirúrgico (o usuário pediu especificamente sobre o nome empresarial neste caso) -- fica registrado aqui para confirmação explícita: se o usuário quiser, é direto aplicar o mesmo reordenamento "antes da comparação" também para a confirmação de situação cadastral, unificando os dois campos no mesmo comportamento "mesma análise".

## 0-N. Cartão CNPJ/QSA anexados de uma empresa diferente da cadastrada -- [SUPERADO/CORRIGIDO na Rodada 26 — ver `CHANGELOG_CORRECOES.md`]

**Atualização (Rodada 26): esta avaliação estava errada e foi corrigida.** O usuário esclareceu explicitamente que o caso do print (Cartão CNPJ "OFICINA DA BELEZA LTDA" numa empresa cadastrada como "43.843.322 ANA AMELIA DA SILVA FREITAS") é o MESMO padrão já resolvido para situação cadastral (Rodada 20): dados desatualizados na API gratuita, documento anexado correto, e o sistema deveria atualizar o cadastro automaticamente -- não um documento de empresa diferente. A Rodada 26 implementou essa correção (`deveConfirmarNomeEmpresarialViaCartao`/`aplicarConfirmacaoNomeEmpresarialDocumentoEmpresa`), com uma trava de segurança adicional baseada em CNPJ para os casos em que o documento é, de fato, de outra empresa. Texto original mantido abaixo, sem edição, só para preservar o histórico da decisão anterior (incorreta).

No mesmo print que motivou a Rodada 25, o Cartão CNPJ anexado ("cnpj-oficina da beleza.pdf") mostra "OFICINA DA BELEZA LTDA" enquanto a empresa cadastrada é "43.843.322 ANA AMELIA DA SILVA FREITAS" -- duas empresas realmente distintas (inclusive de tipo societário diferente: LTDA vs. o padrão de nome com radical de Empresário Individual/MEI). O mesmo arquivo também foi anexado no campo do QSA. O sistema reagiu corretamente nos dois casos: sinalizou "Nome empresarial divergente" no Cartão CNPJ (porque de fato são nomes empresariais diferentes, não uma variação de formatação do mesmo nome) e "Documento incompatível" no QSA (porque um Cartão CNPJ não é um QSA). Isso não é uma regressão nem um falso positivo a corrigir -- é o resultado esperado quando o documento anexado não é o da empresa em questão. Fica registrado aqui só para o usuário confirmar: se a intenção era testar com os documentos reais desta empresa específica ("Ana Amélia da Silva Freitas"), o Cartão CNPJ e o QSA corretos dela precisam ser anexados para a Etapa 1 validar.

## 0-M. Verificação automática da Rodada 24 checa a cada 60 segundos, não instantaneamente -- e só cobre os três tipos com card de identidade (mesmo escopo da Rodada 23)

A Rodada 24 fez a tela checar sozinha, em segundo plano, se uma falha de leitura já pendente (Cartão CNPJ, QSA ou Enquadramento Tributário) foi resolvida pela retentativa automática da Rodada 21 -- sem precisar recarregar a página. O intervalo escolhido foi 60 segundos (bem menor que o cooldown de 15 minutos entre tentativas de leitura, então o usuário nunca espera muito mais do que o necessário depois que a retentativa de fato roda), não instantâneo -- uma verificação a cada segundo seria desnecessariamente pesada para o servidor sem trazer benefício real, já que a própria retentativa só pode acontecer a cada 15 minutos. Da mesma forma que a Rodada 23, esta verificação também cobre só os três tipos com card de identidade dedicado (Cartão CNPJ, QSA, Enquadramento Tributário) -- os cinco tipos sem esse card (ver item 0-L, abaixo) continuam de fora, pelo mesmo motivo já registrado ali. Se o usuário quiser um intervalo mais curto, ou estender a mesma verificação automática aos cinco tipos do item 0-L, é um ajuste de escopo numa próxima rodada, mediante confirmação.

## 0-L. DARF, Faturamento 12 meses, Comprovante de Residência (e variantes) continuam exigindo recarregar a tela para ver o resultado da leitura automática -- não têm um card de identidade nem um mecanismo de polling pronto para reaproveitar

A Rodada 23 fez a leitura automática do Cartão CNPJ/QSA/Enquadramento Tributário aparecer sozinha na tela, sem recarregar, reaproveitando o mecanismo de polling que já existia para o botão "Iniciar análise documental" (`iniciarAnaliseIdentidade`, ligado ao card `StatusAnaliseSlot` desses três tipos específicos). Os outros cinco tipos com leitura automática no servidor (`darf`, `faturamento_12_meses`, `comprovante_faturamento`, `declaracao_faturamento`, `comprovante_residencia` -- ver `TIPOS_COM_ANALISE_AUTOMATICA` em `server/routes/documentos.ts`) não têm um card de identidade equivalente: o resultado da leitura desses aparece só no botão "Dados da análise" de cada arquivo na lista, e não existe hoje nenhum endpoint de status/polling dedicado a eles (só o `GET /api/documentos`, que traz TODOS os documentos da empresa de uma vez, sem filtro por id). Estender a mesma experiência ("Analisando..." e depois o resultado, sem recarregar) a esses cinco tipos exigiria um mecanismo novo (polling do `GET /api/documentos` e atualização pontual só do arquivo recém-anexado no estado local) -- não uma simples reutilização de algo que já existe, como foi possível para a Rodada 23. Não foi feito nesta rodada porque o usuário não relatou nenhum problema especificamente com esses cinco tipos, e para manter o escopo cirúrgico. Fica registrado aqui para confirmação explícita antes de ser implementado, caso o usuário confirme que quer a mesma experiência para esses documentos também.

## 0-K. Campos de endereço do Cartão CNPJ saem misturados/incorretos na extração local -- mesmo tipo de layout em colunas já corrigido para telefone/e-mail e situação/data, mas não reportado pelo usuário

Ao testar a extração local (`extrairDocumentoLocal`) contra o PDF real do Cartão CNPJ desta rodada, os campos de endereço saíram claramente incorretos -- por exemplo, `numero` veio como "29.705.345/0001-22" (o próprio CNPJ) e `cep` veio como "BAIRRO/DISTRITO MUNICÍPIO UF" (um rótulo, não um valor). A causa é a mesma classe de problema já corrigida nesta rodada para "situação cadastral"/"data da situação cadastral" e, na Rodada 21, para "endereço eletrônico"/"telefone": o Cartão CNPJ oficial imprime vários rótulos de endereço lado a lado na mesma linha (`logradouro`/`número`/`complemento`, `cep`/`bairro`/`município`/`uf`), e `valorAposRotulo` (o mecanismo genérico de "pega a linha inteira depois do rótulo") não sabe separar colunas dentro da mesma linha. O usuário não relatou nenhum problema relacionado a endereço nesta conversa -- só situação cadastral, nome e contato --, então essa correção não foi feita agora, para manter o escopo cirúrgico e não mexer em código que não foi pedido. Se o usuário confirmar que precisa do endereço extraído corretamente do Cartão CNPJ (por exemplo, para preencher automaticamente o endereço da empresa), o próximo passo é aplicar o mesmo padrão de extração dedicada por regex já usado para telefone/e-mail (`extrairContatoCartaoCnpj`) a esse grupo de campos.

## 0-J. Janela de 5 dias desta rodada vale só para a correção de situação cadastral -- telefone/e-mail continuam na janela de validade documental de 30 dias já existente desde a Rodada 21

O pedido desta rodada ("o documento para atualização dos dados não pode ter mais de 5 dias da consulta e emissão, isso é só se a empresa tiver alterações e a API da Receita ainda não estiver atualizada") foi interpretado como específico da correção de **situação cadastral**, porque menciona explicitamente "a API da Receita ainda não estiver atualizada" -- e a atualização de telefone/e-mail nunca depende da API da Receita ficar desatualizada, porque as fontes gratuitas de CNPJ (BrasilAPI, CNPJá Open, OpenCNPJ) nunca trazem telefone/e-mail, em nenhuma hipótese (ver Rodada 21). Por isso, `deveAtualizarContatoViaCartao`/`aplicarAtualizacaoContatoDocumentoEmpresa` continuam usando a janela de validade documental de 30 dias já existente, sem a exigência extra de 5 dias que passou a valer só para `deveConfirmarSituacaoCadastralViaCartao`. Se o usuário quiser a mesma janela apertada de 5 dias também para telefone/e-mail (por exemplo, por entender que um Cartão CNPJ "velho" não deveria mais atualizar contato automaticamente, independentemente de haver ou não uma correção de situação cadastral em jogo), é um ajuste direto de escopo -- fica registrado aqui para confirmação explícita antes de estender, em vez de mudar um comportamento que não foi pedido nestes termos.

## 0-H. Retentativa automática desta rodada cobre só o Cartão CNPJ -- QSA/Enquadramento/Atos da Junta têm o mesmo padrão arquitetural e podem precisar do mesmo tratamento

A Rodada 21 (ver `CHANGELOG_CORRECOES.md`) corrigiu, para o Cartão CNPJ, uma falha de leitura persistida que ficava sendo reexibida para sempre em toda visualização normal da tela, sem nunca ser tentada de novo sem um clique manual em "Analisar documentos". QSA, Enquadramento Tributário e Atos da Junta Comercial (`montarQsaDocumentalDados`, `montarEnquadramentoDados`, `montarAtosJuntaDados`, todos em `server/routes/documentacao.ts`) têm exatamente o mesmo padrão -- usam `obterAnaliseEspecializada({ processar, reprocessar: processar })` e, quando não há análise persistida e `processar=false` (visualização normal), caem para `buscarFalhaAnaliseEspecializada`, reexibindo uma falha antiga sem nunca tentar de novo sozinhos. Esta rodada resolveu apenas o Cartão CNPJ porque foi o caso concretamente investigado e comprovado contra o documento real do usuário; o mesmo mecanismo (`deveReprocessarCartaoCnpjAutomaticamente`/`deveRetentarAnaliseFalhaAutomaticamente`, `server/utils/retentativaAutomaticaAnaliseDocumental.ts`) pode ser generalizado para os outros três tipos numa rodada futura, se o usuário confirmar que os viu com o mesmo sintoma (uma mensagem de erro antiga que nunca se atualiza sozinha, mesmo depois de reaberta a tela várias vezes). Fica registrado aqui para confirmação explícita antes de estender, em vez de mexer em três fluxos adicionais por suposição.

## 0-I. Causa exata de "0 sócios lidos" no QSA da empresa do print (Rodada 21) não foi confirmada -- falta acesso ao documento QSA real dela

O print da Rodada 21 mostrou o QSA da empresa Empresário Individual com "Sócios lidos no QSA: 0" e "A razão social do QSA diverge da razão social sincronizada". A correção desta rodada resolveu com evidência forte a segunda parte (o radical do CNPJ no nome, ver `CHANGELOG_CORRECOES.md`). A primeira parte ("0 sócios") já tem uma regra correta desde uma correção anterior (31/08/2026): para Empresário Individual, "zero sócios" é o resultado ESPERADO e correto (o titular não é um "sócio" no sentido societário), refletido pelo campo `qsa_nao_aplicavel` quando o próprio documento traz esse texto explícito ("A NATUREZA JURÍDICA NÃO PERMITE O PREENCHIMENTO DO QSA"). Não foi possível confirmar, sem acesso ao arquivo QSA real anexado a essa empresa em produção (diferente do Cartão CNPJ, cujo PDF real já estava disponível para esta engenharia desde uma rodada anterior), se o "0 sócios" do print é só esse caso esperado (e a interface deveria estar tratando isso como algo positivo, não como pendência) ou se há também uma falha de leitura própria do documento QSA dessa empresa. Se o usuário observar o mesmo sintoma depois deste deploy, anexar o QSA real dessa empresa permite uma investigação direta, no mesmo padrão usado para o Cartão CNPJ nesta rodada.

## 0-G. Cartão CNPJ não grava `motivo_situacao_cadastral` na confirmação/trava da Rodada 20 -- decisão de escopo, documento não fornece esse dado hoje

A correção da Rodada 20 (ver `CHANGELOG_CORRECOES.md`) só grava `situacao_cadastral`/`data_situacao_cadastral` a partir da leitura do Cartão CNPJ -- `motivo_situacao_cadastral` não é tocado, porque a estrutura de extração do documento (`ExtracaoCartao`, `server/services/analiseCnpjReceitaCartao.ts`) não tem hoje um campo para "motivo" (não está no prompt de leitura nem no parser local). Para o caso pedido (confirmar ATIVA) isso não é uma lacuna prática, porque uma empresa ativa normalmente já não tem motivo cadastrado. Se o negócio quiser, no futuro, que o Cartão CNPJ também confirme/trave um motivo de situação irregular (quando o documento mostrar isso explicitamente), é preciso primeiro estender a extração do documento para ler esse campo -- fica registrado aqui para confirmação explícita antes de ser implementado, em vez de inventar uma leitura que o documento não fornece hoje.

## 0-F. Consulta "tempo real" paga para confirmação pontual antes de um envio crítico ao banco -- decisão de custo, não implementada por não caber a esta engenharia decidir gastar dinheiro do negócio sem confirmação

A Rodada 19 implementou a sincronização automática de CNPJ com custo zero (reconsulta em segundo plano as mesmas três fontes gratuitas já integradas -- BrasilAPI, CNPJá Open, OpenCNPJ -- para empresas com situação não-ativa ou nunca sincronizadas, ver `CHANGELOG_CORRECOES.md`). Isso reduz o atraso de "nunca, até alguém lembrar de clicar em Atualizar cadastral" para "algumas horas" (padrão: 6h para quem está com situação não-ativa) -- mas não elimina a janela própria de cada fonte gratuita (a CNPJá Open, por exemplo, documenta publicamente até 45 dias para refletir uma mudança de situação cadastral em estabelecimentos ativos; nenhuma fonte gratuita ou governamental de uso livre para empresa privada garante tempo real, como detalhado na mesma seção do changelog).

Se o negócio precisar de confirmação em minutos para um momento pontual e crítico -- por exemplo, checar a situação cadastral no instante exato de enviar uma proposta ao banco, não para o portfólio inteiro em segundo plano --, a única forma real é contratar uma consulta paga "tempo real" sob demanda (ex.: CNPJá Comercial com o parâmetro `maxAge=0`, ou provedor equivalente como ReceitaWS PRO/CNPJ.ws PRO), cobrada por consulta. Isso não foi implementado nesta rodada porque envolve gasto recorrente de dinheiro do negócio e escolha de fornecedor -- uma decisão do usuário/negócio, não uma escolha técnica que esta engenharia deva tomar sozinha. Fica registrado aqui para confirmação explícita: se o usuário quiser, uma rodada futura pode adicionar essa consulta paga como um botão específico "Confirmar situação agora" no momento de enviar a proposta (não como substituto da sincronização automática gratuita, que já cobre o caso geral relatado).

**Reafirmado na Rodada 20:** o usuário confirmou explicitamente, ao relatar a regressão da própria Rodada 19, que quer continuar usando só a API gratuita por enquanto ("Por enquanto que, vamos continuar usando a API gratuita, sem passar pela API paga") -- este item segue pendente de confirmação, sem nenhuma mudança, exatamente como já estava.

## 0-E. Cruzamento DIRETO entre Cartão CNPJ e QSA (comparar os dois documentos entre si) não existe -- hoje o cruzamento é indireto, via Receita Federal -- decisão de escopo pendente de confirmação

A mensagem de voz da Rodada 18 pediu que, depois de ler os dados do documento anexado, o sistema "confira com o QSA, se tudo bate, se tudo está correto de acordo com o CNPJ". Hoje, tanto o Cartão CNPJ (`analiseCnpjReceitaCartaoEmpresa`) quanto o QSA (`analisarQSA`) já comparam seus próprios dados contra a mesma fonte de verdade -- os dados da empresa sincronizados a partir da Receita Federal (`empresas`/`socios_empresa`) -- então uma divergência em qualquer um dos dois já é pega hoje, mesmo sem uma rotina que compare os dois documentos diretamente entre si. Uma comparação DIRETA (ex.: mesmo CNPJ extraído nos dois arquivos, capital social do Cartão CNPJ batendo com o do QSA quando esse dado aparece em ambos) seria um reforço adicional -- mais como uma segunda camada de garantia do que a correção de um problema concreto observado nos prints desta rodada, já que nenhuma divergência real desse tipo foi reportada. Ver `CHANGELOG_CORRECOES.md`, seção "Rodada 18", nota final. Fica registrado aqui para confirmação explícita antes de ser implementado numa rodada futura.

## 0-D. Confirmação da Etapa 1 só aparece na próxima carga de tela, não instantaneamente após o upload -- decisão de escopo consciente [RESOLVIDO nas Rodadas 23/24]

**Atualização (Rodada 23/24): esta lacuna foi fechada.** A Rodada 23 fez o resultado aparecer sozinho, sem recarregar, no exato momento de um upload nesta mesma aba; a Rodada 24 cobriu o caso restante (uma falha já pendente de antes, sem upload novo) com uma verificação automática em segundo plano (ver item 0-M). Texto original mantido abaixo, sem edição, para preservar o histórico da decisão.

A Rodada 17 corrigiu a causa raiz de a Etapa 1 (QSA/Enquadramento Tributário) continuar pedindo o clique manual em "Iniciar análise documental" mesmo depois de a leitura automática do upload já ter concluído -- ver `CHANGELOG_CORRECOES.md`, seção "Rodada 17". A correção grava o resultado da leitura automática no mesmo lugar (`documentos_extracoes_ia`) que a Etapa 1 já consulta em toda carga de página, então a confirmação passa a aparecer sozinha na PRÓXIMA vez que a tela carregar (reabrir a página, navegar de volta) -- não instantaneamente, no mesmo segundo em que o upload termina, sem nenhum recarregamento. O pedido do usuário foi literal ("sem precisar iniciar a análise documental"), não sobre latência; implementar a instantaneidade completa exigiria algum tipo de polling/recarregamento automático depois do upload, o que entraria em tensão direta com a correção da Rodada 16 (upload não recarrega mais a tela). Fica registrado aqui para confirmação explícita, caso o usuário queira esse refinamento adicional numa rodada futura.

**Nota sobre a Rodada 16:** quatro melhorias de UX pedidas para o Acervo Documental da empresa -- upload sem recarregar a tela inteira, remoção do botão de validação manual, grid mais compacto (4-5 campos por linha) e correção de um bug que fazia o toggle "Ver documentos complementares" perder o estado a cada anexo -- ver `CHANGELOG_CORRECOES.md`, seção "Rodada 16".

## 0-C. Leitura automática no upload cobre só um subconjunto de tipos de documento -- decisão de escopo pendente de confirmação, não implementada por suposição

A mensagem do usuário na Rodada 16 descreve o fluxo desejado como "a leitura de cada documento é automática, mas a análise para o dossiê completo é somente acionada depois que a documentação necessária estiver completa". A parte do dossiê completo (dependente de o usuário clicar em "Iniciar análise documental"/"Iniciar análise societária") já funcionava assim antes desta rodada, e a Rodada 16 reforçou isso ao parar de recalcular o dossiê a cada upload isolado (ver `CHANGELOG_CORRECOES.md`). Mas a primeira parte -- "a leitura de cada documento é automática" -- só é totalmente verdadeira hoje para um subconjunto de tipos (`TIPOS_COM_ANALISE_AUTOMATICA`, `server/routes/documentos.ts`: Cartão CNPJ, QSA, Enquadramento Tributário/Simples Nacional, Faturamento, Comprovante de Residência). Os demais tipos do catálogo (SCR, CCS, CCF, CENPROT, CND RFB, CADIN, PGFN, ECF, DEFIS, PGDAS/PGMEI, Rating, situação fiscal etc.) continuam exigindo o botão "Reanalisar" por arquivo (Rodada 5) ou a geração do relatório para serem lidos pela primeira vez -- eles não são lidos sozinhos assim que o arquivo é anexado.

Estender a leitura automática no upload para todos esses tipos é tecnicamente viável (o mesmo padrão de `agendarAnaliseRegraDocumental` pode ser generalizado), mas é uma decisão de produto com custo operacional real: significa disparar uma chamada de IA/OCR a cada anexo desses ~15 tipos adicionais, em vez de sob demanda (quando o usuário pede o relatório ou clica "Reanalisar") -- mais custo de API por empresa cadastrada, e mais superfície para falhas assíncronas silenciosas caso o provedor de IA esteja indisponível no momento do upload. Como a mensagem do usuário não pediu essa extensão com essa especificidade (o exemplo dado foi sobre Cartão CNPJ/QSA/Enquadramento, que já são automáticos), não foi implementada por suposição -- fica registrada aqui para confirmação explícita antes de uma rodada futura que a implemente.

**Nota sobre a Rodada 15:** o usuário reportou "continua com erro" na MESMA empresa da Rodada 13 (QSA de Empresário Individual) -- a regra já tinha sido corrigida na Rodada 13, mas o `RULE_VERSION` nunca foi bumpado, então o laudo antigo persistido continuava sendo servido como atual. Corrigido (bump de `RULE_VERSION`, mais uma checagem de obsolescência nova em `montarQsaDocumentalDados`/`montarEnquadramentoDados`, mais um bloqueio duplicado no QSA igual ao já corrigido para Enquadramento na Rodada 14) -- ver `CHANGELOG_CORRECOES.md`, seção "Rodada 15". **Ação operacional obrigatória após o deploy desta rodada:** rodar `npm run backfill:laudos -- enqueue-and-run` (ou usar "↻ Forçar nova leitura" nos documentos específicos) -- sem isso, o bump de versão sozinho não muda o que a tela mostra.

## 0-B. Mesma checagem de laudo desatualizado provavelmente falta em `montarAtosJuntaDados` e no caminho de Contrato Social de `montarValidacaoSocietaria` (Etapa 2) -- escopo deixado de fora da Rodada 15 por falta de evidência direta

A Rodada 15 corrigiu a ausência de checagem de obsolescência (`analiseDesatualizada`) nos dois agregadores de Etapa 1 (`montarQsaDocumentalDados`, `montarEnquadramentoDados`) -- mas essa mesma lacuna estrutural provavelmente também existe em `montarAtosJuntaDados` e no trecho que lê o Contrato Social dentro de `montarValidacaoSocietaria` (ambos usados na Etapa 2, validação societária), que também consomem `obterAnaliseEspecializada`/`buscarAnaliseEspecializadaPersistida` sem checar `analysis_status`/`reprocessamento_necessario` do resultado. Não foi corrigido nesta rodada porque não há evidência direta, na captura de tela atual, de que esses dois caminhos estejam hoje mostrando uma pendência calculada por regra desatualizada (o Atos da Junta aparece com selo verde de validado para a empresa do caso reportado) -- seguindo o princípio cirúrgico desta engenharia, alterar código sem um sintoma reproduzível concreto arrisca mais do que resolve. Fica registrado para ser corrigido, com o mesmo padrão já usado nesta rodada (`analiseDesatualizada()`, já exportável de `documentacao.ts`), assim que houver um caso real reportado nessas duas telas.

**Nota sobre a Rodada 14:** o usuário trouxe uma nova empresa (Simples Nacional optante) mostrando a tela "bagunçada e poluída" e pediu que o sistema de documentos/análises funcione e tenha o mesmo padrão visual para qualquer tipo de empresa. Quatro causas raiz concretas foram encontradas e corrigidas -- ver `CHANGELOG_CORRECOES.md`: nome de sócio duplicado no QSA (`formatarSocio`), dois avisos idênticos sobre Atos da Junta não anexado, dois avisos idênticos sobre pendência do Enquadramento Tributário ("4 avisos" no print), e o código interno do motor de extração exposto cru em "Fonte da leitura".

## 0-A. Possível cabeçalho de card do Enquadramento Tributário mostrando nome de arquivo bruto -- observado, não confirmado

Ao analisar as capturas de tela da empresa "B1 SAUDE E ESTETICA LTDA" (Rodada 14), havia indício visual de que o card de Enquadramento Tributário pudesse estar usando o nome do arquivo enviado pelo usuário como título, em vez de um rótulo de tipo fixo ("Enquadramento Tributário"). A investigação no código (`StatusAnaliseSlot`, `ProntidaoIdentidadeCard`, `DocumentosEntidade.tsx`) não encontrou, com confiança suficiente, um ponto concreto onde isso aconteça -- os títulos de card e de seção que foram auditados já usam rótulos fixos (`item.nome`, vindo do backend com valor fixo `'Enquadramento Tributário'`, nunca do nome do arquivo). Diferente dos 4 itens corrigidos na Rodada 14 (cada um confirmado por reversão temporária contra um sintoma reproduzível), este item não teve uma causa raiz localizada e reproduzível -- por isso não foi alterado nenhum código para ele. Fica registrado para ser retomado com uma nova captura de tela mais específica (ex.: destacando exatamente qual elemento da tela mostra o nome do arquivo) ou um exemplo reproduzível em ambiente de teste.

**Nota sobre a Rodada 13:** o usuário trouxe documentos reais de uma empresa (Empresário Individual) mostrando três problemas no Acervo Documental. Dois eram bugs de código, corrigidos nesta rodada -- ver `CHANGELOG_CORRECOES.md`: (1) o QSA de uma empresa Empresário Individual, cuja resposta oficial da Receita ("A natureza jurídica não permite o preenchimento do QSA") era tratada como falha em vez de resposta completa; (2) o campo "Status da leitura" mostrando um flag administrativo/manual ("✓ Validar") sob um rótulo que implica confirmação da leitura automática, mesmo sem nenhum laudo real. O terceiro item (Enquadramento Tributário e Atos da Junta Comercial travados em "Aguardando análise" nesta empresa específica) não é um bug de regra de negócio identificado -- é a análise automática que genuinamente não completou para esses dois arquivos; a Rodada 13 corrigiu o diagnóstico (a tela agora mostra o motivo real quando existe uma falha persistida) e registra, no item novo abaixo, uma lacuna estrutural mais profunda encontrada durante a investigação.

## 0. Análises documentais que falham não são reprocessadas automaticamente

Ao investigar por que dois documentos de uma empresa real ficaram travados em "Aguardando análise" indefinidamente (Rodada 13), foi confirmado que `executarAnaliseDocumentalEspecializada` (`server/routes/documentacao.ts`) grava `next_retry_at = NOW() + INTERVAL '5 minutes'` quando uma análise falha (erro de rede, indisponibilidade momentânea do provedor de IA, etc.) -- mas nada no sistema hoje volta a consultar essa coluna para de fato reprocessar. Uma falha transitória, sem uma ação manual do usuário (o botão "↻ Forçar nova leitura", já existente no Acervo Documental), permanece pendente para sempre. Corrigir isso corretamente exige infraestrutura nova (um worker/cron periódico que consulte `WHERE next_retry_at <= NOW() AND status = 'falhou'` e chame `executarAnaliseDocumentalEspecializada` novamente, com um limite de tentativas), o que está fora do escopo de uma correção cirúrgica pontual -- é uma peça de infraestrutura assíncrona nova, não um ajuste de uma regra existente. Fica registrado aqui para ser tratado como seu próprio item de trabalho.

**Nota sobre a Rodada 12:** duas capturas de tela com um círculo desenhado à mão em volta das pílulas "Ação necessária"/"Avisos" trouxeram três pedidos concretos, todos implementados nesta rodada -- ver `CHANGELOG_CORRECOES.md`: (1) selo "Avisos" removido da Etapa 1 pendente (redundante com "Ação necessária"); (2) texto do popover de pendência de regime reescrito para instruir a ação diretamente; (3) terceiro botão "Outro", genérico, para qualquer documento que comprove explicitamente o regime tributário, quando a empresa não tem ECF/DCTF/DARF/Livro Caixa.

**Nota sobre a Rodada 11:** feedback com print da tela apontando que "Dados da análise" ainda exigia um clique para revelar "Documento inválido..." em documento incompatível -- corrigido, agora aparece direto, sem clique. O pedido de transição de regime tributário da Rodada 10 foi refinado com uma janela de 12 meses (a exceção só vale enquanto a transição for recente) -- ver `CHANGELOG_CORRECOES.md`. O terceiro ponto da mesma mensagem (leitura cruzada de todos os documentos, textos objetivos) é uma reafirmação de princípios já em vigor, sem um caso concreto reproduzível -- nenhuma alteração de código foi necessária; fica registrado para virar uma correção cirúrgica assim que houver um exemplo específico.

**Nota sobre a Rodada 8:** as três críticas do usuário sobre a tela gerada pela Rodada 7 (texto do alerta longo demais, dois alertas duplicados para o mesmo problema, selo "Revisão necessária" em vez de "Documento incompatível") foram todas implementadas nesta rodada -- ver `CHANGELOG_CORRECOES.md`.

**Nota sobre a Rodada 9:** feedback imediatamente seguinte do mesmo usuário foi além da Rodada 8 -- pediu que NENHUM dado do documento errado apareça na tela, nem resumido; só "Documento inválido, anexe o [tipo esperado]". Implementado nesta rodada -- ver `CHANGELOG_CORRECOES.md`.

**Nota sobre a Rodada 10:** o usuário confirmou a Rodada 9 ("Exatamente isso") e trouxe dois pedidos novos: (1) documentos do regime tributário anterior continuarem sendo pedidos quando a empresa mudou de regime -- **implementado nesta rodada, ver item 13 abaixo, que passa de "pendente" para "resolvido"**; (2) reafirmação de que o conteúdo do arquivo, nunca o nome ou o slot, decide a identidade do documento -- **auditado nesta rodada e confirmado já garantido pela arquitetura existente, sem necessidade de alteração de código** (ver `CHANGELOG_CORRECOES.md`, seção "Rodada 10").

Lista honesta do que a missão pediu e NÃO foi implementado nesta sessão, com a razão concreta de cada omissão. Nada aqui está descartado por "fora de escopo" sem justificativa -- cada item abaixo é trabalho real, mapeado, que exige mais que uma correção cirúrgica para ser feito com segurança.

## 1. Vocabulário completo de status documentais como enum central

A missão pede doze status (`PENDENTE`, `VALIDADO`, `VALIDADO_COM_ALERTA`, `VALIDADO_HISTORICO`, `INCOMPATIVEL_COM_CAMPO`, `NAO_APLICAVEL`, `NAO_APLICAVEL_AO_REGIME`, `FORA_DA_JANELA_ATUAL`, `AINDA_NAO_EXIGIVEL`, `SATISFEITO_POR_DOCUMENTO_EQUIVALENTE`, `REVISAO_HUMANA`, `REPROVADO_DOCUMENTALMENTE`) usados de forma consistente em todo o sistema. Hoje, cada parte do código tem seu próprio vocabulário pontual (`documento_compativel: boolean`, `regime_confirmado: boolean`, `status_analise: 'ANALISE_ESPECIALIZADA_PENDENTE'`, os `codigo`s de alerta como `regime_tributario_codigo_nao_mapeado`). Unificar isso num enum central exigiria: (a) decidir, documento por documento, qual status granular se aplica a cada combinação de estado hoje representada por booleans espalhados; (b) atualizar toda a API que devolve esses campos para o frontend; (c) atualizar o frontend (`DocumentosEntidade.tsx` e outras telas) para consumir o novo vocabulário sem quebrar a experiência atual; (d) decidir se e como persistir esse status no banco (nova coluna em `documentos_arquivos`? tabela derivada?). É trabalho de arquitetura de várias rodadas, não uma correção cirúrgica -- implementá-lo às pressas nesta rodada arriscaria exatamente o tipo de regressão silenciosa que esta missão inteira busca eliminar.

**O que já existe, pontualmente, como sementes desse vocabulário:** `ANALISE_ESPECIALIZADA_PENDENTE` (EFD-Contribuições), `REVISAO_HUMANA`/`CODIGO_NAO_MAPEADO` (alerta do DARF 8998), `SATISFEITO`/`CERTIDAO_POSITIVA_COM_EFEITO_NEGATIVO`/`CERTIDAO_POSITIVA`/`NAO_APLICAVEL`/`PENDENTE` (cobertura de bureau, `coberturaEvidenciaBureauService.ts`). Uma futura unificação pode partir daqui em vez de do zero.

## 2. Integração automática das capacidades novas da Rodada 3 aos fluxos de análise existentes

`registrarPeriodoRegime` (Rodada 2), `registrarFaturamentoCompetencia` e `registrarCoberturaEvidencia` (Rodada 3) são funções prontas, testadas e com rota de leitura -- mas nenhuma delas é chamada automaticamente quando um documento é analisado hoje (`analisarSimplesNacional`, `analisarDocumentoCatalogado` etc.). Ou seja: a infraestrutura de gravação existe, mas hoje só é populada se alguém chamar essas funções manualmente (ex.: via script ou futura chamada explícita). Conectar isso ao pipeline de análise real exige decidir, para cada tipo de documento, QUAL requisito/competência/período ele evidencia e QUANTA confiança atribuir -- uma decisão de produto e não só de código, que merece revisão dedicada em vez de ser assumida nesta rodada.

## 3. Classificador de tipo de comprovante de regime cobre só 4 tipos (e o novo `situacao_certidao` da Rodada 4 depende da IA, não é determinístico)

`detectarTipoComprovanteRegime` reconhece `ecf`, `dctf_mit`, `darf`, `livro_caixa` -- os tipos que a missão pediu explicitamente na matriz cruzada (seção 47). Os demais tipos documentais do catálogo (CND, CADIN, CRF, SCR, PGDAS, DEFIS) ainda não têm um classificador de identidade independente do slot equivalente -- continuam usando a checagem de compatibilidade genérica de `normalizarDocumentoCatalogado` (`bruto.documento_compativel === false`), que depende inteiramente do que a IA/OCR retornou, sem uma segunda camada determinística de verificação de tipo real. Extender o classificador para esses tipos é viável, mas cada um tem marcadores textuais próprios que precisam ser levantados com cuidado (o mesmo processo usado para os 4 tipos desta rodada) para não introduzir falsos negativos.

**Atualização Rodada 4:** para a categoria `cnd_cpend` (CND/CPEND Federal, PGFN, CADIN) especificamente, foi acrescentado um campo `situacao_certidao` exigido explicitamente no prompt da IA e convertido em alerta obrigatório (`certidao_situacao_positiva` / `certidao_situacao_nao_identificada`) sempre que o resultado não seja claramente negativo -- isso responde ao bug real encontrado (CADIN "incluído" tratado como válido). Essa correção NÃO é determinística: ela depende da IA extrair `situacao_certidao` corretamente, ao contrário do classificador local (`detectarTipoComprovanteRegime`) usado para ECF/DCTF/DARF/Livro Caixa, que lê o texto diretamente com regex e nunca depende de uma chamada externa. Construir um classificador determinístico equivalente para `cnd_cpend` exigiria ligar `extrairDocumentoLocal` a essa categoria (hoje ela pula direto para a IA, `usarExtracaoLocal = false`) SEM substituir a extração rica que a IA hoje devolve (razão social, número do documento, órgão emissor etc.) -- arriscado demais para fazer às pressas numa correção de urgência. Prioridade real para uma próxima rodada dedicada.

## 4. EFD-Contribuições: leitura especializada de M400/M800 continua não implementada

A Rodada 3 tornou a limitação EXPLÍCITA (`ANALISE_ESPECIALIZADA_PENDENTE`), mas não implementou a leitura em si. Construir essa leitura exige validar a estrutura real dos registros M400/M800 com um documento de referência real -- um leitor construído sem essa validação arrisca introduzir exatamente o tipo de erro silencioso (um número de receita bruta calculado errado, mas com aparência de confiável) que esta missão busca eliminar. Prioridade real para a próxima rodada, com escopo mapeado em `DIAGNOSTICO_MASTER_PROMPT_CREDITO.md`, item 3.6.

## 5. `efd_icms_ipi` não foi tocado

A EFD ICMS/IPI é uma obrigação diferente (ICMS/IPI, não PIS/COFINS) e não foi mencionada pela missão desta rodada -- deliberadamente fora do escopo do item 6 acima (EFD-Contribuições). Se a mesma limitação se aplicar a ela, precisa de uma auditoria própria.

## 6. Motor de objeção / e-mail de defesa técnica

Mencionado na missão original (Rodada 1) como parte da arquitetura completa; não iniciado em nenhuma das três rodadas desta sessão. Nenhum código relacionado existe hoje no repositório para servir de base.

## 7. Catálogo único com teste de consistência banco/backend/frontend (visão completa)

`tests/catalogoDarfConsistencia.test.ts` (Rodada 3) cobre a consistência entre o catálogo de código de receita do DARF e o prompt da IA -- um caso concreto e de alto valor (é a causa raiz dos dois bugs de regime corrigidos nesta sessão). `tests/tiposDocumentoCatalogo.test.ts` (pré-existente) cobre a consistência entre o catálogo de tipos documentais e a whitelist de upload. Uma consistência mais ampla -- cruzando `shared/documentTypes.ts`, toda constraint de banco relacionada a tipo de documento, e todo componente de frontend que lista tipos documentais -- não foi construída; os dois testes existentes cobrem os pontos de maior risco já identificados nesta sessão, não a superfície completa.

## 8. Bureaus: classificador textual cobre os marcadores mais comuns, não uma extração estruturada

`detectarRequisitosCobertosPeloTexto` reconhece os requisitos pela presença de palavras/siglas no texto (SCR, CCS, CCF, CENPROT, CADIN, PGFN, CND federal, CNDT, Situação Fiscal, Serasa) -- não faz uma extração estruturada de campos (número do relatório, data de emissão, órgão). É suficiente para o propósito desta rodada (provar que um documento cobre múltiplos requisitos), mas não substitui uma leitura completa de cada tipo de bureau.

## 9. Relatório de Situação Fiscal (CNPJ/CPF): sem checagem de mérito dedicada

Documento real da empresa ZR CONSTRUCOES E REFORMAS CIVIS LTDA (CNPJ 49.366.887/0001-25), anexado no slot "Relatório de Situação Fiscal (CNPJ)", mostra parcelamento em atraso (3 parcelas) e débitos de PIS/COFINS em aberto -- uma situação fiscal claramente desfavorável. Esse tipo (`situacao_fiscal_cnpj`/`situacao_fiscal_cpf`) tem sua própria categoria de análise (`analise` distinta de `cnd_cpend`, não coberta pela correção da Rodada 4) e continua dependendo inteiramente da extração genérica da IA para os campos `situação`/`campos_comprovados`, sem nenhum alerta dedicado equivalente ao `certidao_situacao_positiva` criado para CND/CADIN/PGFN nesta rodada. Levantar os marcadores textuais próprios de um "Diagnóstico Fiscal na Receita Federal" (que é estruturalmente diferente de uma certidão negativa/positiva -- é uma lista de pendências de parcelamento e débito, não um resultado binário) exige o mesmo cuidado dedicado já aplicado aos outros tipos, e não uma extensão apressada da lógica de `situacao_certidao`.

## 10a. Botão "Reanalisar" novo não reprocessa em lote automaticamente

A Rodada 5 acrescentou um botão "Reanalisar" por arquivo nos documentos catalogados genéricos, mas ele é manual, um arquivo por vez -- não existe (ainda) uma ação em lote ("reanalisar todos os documentos desta empresa" ou "reanalisar todos os documentos já lidos antes de tal data"). Para uma base com muitas empresas já com documentos analisados por versões antigas do motor, isso significa clicar documento por documento. Uma rotina de reprocessamento em lote (varrer `documentos_extracoes_ia` com `status = 'concluido'` e comparar `prompt_versao` com a versão atual, chamando `/extrair` para cada um fora de horário de pico) é viável, mas é uma migração de dados operacional, não uma correção cirúrgica de UI -- fica como próximo passo natural se o volume de documentos com laudo desatualizado for grande.

## 10. Descompasso entre o código no GitHub e o que roda em produção

O print do campo "Enquadramento tributário" mostra `FONTE DA LEITURA:
local:reextract-v1` -- uma string que não existe em nenhuma versão deste
repositório, em nenhuma rodada desta sessão. Isso é evidência concreta de
que o site em produção (destravacredito.com) está rodando um código
diferente do que está hoje em `vml-arquivos/destrava` branch `main`. Esta
sessão nunca teve acesso de push/deploy (o usuário sempre aplicou os zips
manualmente via Coolify), então não é possível confirmar a partir daqui se
há um ajuste manual direto no servidor, uma branch diferente em produção,
ou uma versão desatualizada com nomenclatura antiga -- só recomendar que
isso seja verificado antes de assumir que o deploy do zip desta entrega vai
produzir exatamente o comportamento descrito nos changelogs desta sessão.

## 11. Duas observações do relatório da ZR CONSTRUÇÕES ainda não investigadas (podem ou não ser o mesmo bug já corrigido)

Ao ler o PDF real do relatório desta empresa para confirmar a correção do item acima (Enquadramento Tributário duplicado), duas outras coisas chamaram atenção e **não foram investigadas nesta rodada** -- citadas aqui por transparência, não por terem sido descartadas:

- **"atos da junta.pdf" (Validado) e "ATOS DA JUNTA.pdf" (Excluído) aparecem juntos na seção "1. Documentos anexados e analisados".** São dois arquivos DIFERENTES (um ativo, um excluído) do mesmo tipo -- ao contrário do bug corrigido nesta rodada, aqui o `tipo_documento` é idêntico nos dois casos, então a chave de deduplicação já é a mesma (`atos_junta_comercial`); o que não foi confirmado é se um documento com `status = 'excluido'` deveria sequer entrar na lista que alimenta essa seção do relatório (`documentosAnexados`, que hoje não filtra por status de exclusão antes de aplicar a deduplicação por pontuação). Não foi corrigido porque não há evidência de que seja um bug -- pode ser intencional (mostrar o histórico de tentativas) -- e mexer nisso sem confirmar a intenção correta arriscaria esconder informação que o usuário queira ver.
- **Possível contradição entre o card do Enquadramento Tributário (dados confiantes: Não Optante, confiança 90%) e a seção "4. Resultados consolidados por etapa → Identidade do CNPJ"**, que no mesmo relatório mostra "Observações: Não foram encontrados campos comprovados nem evidências suficientes" e "Pendências e bloqueios: Regime tributário não identificado. Sincronize os dados de CNPJ (Receita Federal) da empresa." Essas duas seções usam fontes de dados diferentes dentro do mesmo `dossie` (`identidade_cnpj.documentos_iniciais.enquadramento_tributario`, calculado por uma rotina de validação separada da Etapa 1, versus a leitura direta do arquivo usada no card da seção 1) -- não foi confirmado se é o mesmo tipo de dessincronia do bug corrigido nesta rodada ou uma causa raiz diferente, e não há reprodução construída ainda.

Ambos os pontos merecem uma rodada dedicada, com reprodução própria, em vez de uma correção apressada sobre uma leitura de PDF sem confirmação de código.

## 12. Arquitetura de classificação central/versionamento de laudos chegou ao repositório sem changelog próprio

Ao investigar esta rodada, a cópia do repositório usada como base já continha `classificadorDocumentalCentral.ts`, `documentalLaudoVersioning.ts`, `backfillLaudosService.ts`, `scripts/backfill-laudos.ts` e a migration 103 -- peças substanciais e corretas, mas que nunca apareceram em nenhuma rodada deste `CHANGELOG_CORRECOES.md` nem de `FILES_CHANGED.txt` antes de agora. Isso sugere que parte do trabalho desta sessão foi feito fora do fluxo desta conversa (outra sessão, outro desenvolvedor, ou um merge manual) sem atualizar a documentação de acompanhamento. Registrado aqui por transparência -- `FILES_CHANGED.txt` desta entrega documenta a lacuna explicitamente em vez de fingir uma certeza que não existe sobre o diff completo desde a Rodada 6. Recomendação: antes da próxima rodada, alinhar com quem quer que tenha feito essas adições (ou confirmar que foi você mesmo, através de outra sessão) para manter as duas fontes de documentação -- esta e o histórico real do repositório -- sincronizadas.

## 13. Visibilidade condicional dos campos de documento por regime tributário -- RESOLVIDO NA RODADA 10

Pedido original (registrado numa rodada anterior desta sessão): se o regime tributário atual da empresa é Lucro Real/Presumido/Arbitrado, os campos específicos de Simples Nacional/MEI (PGDAS/PGMEI, DEFIS/DASN-SIMEI etc.) não deveriam nem aparecer na tela -- e vice-versa. A exceção explícita: quando a empresa MUDOU de regime, o campo do regime ANTERIOR precisa continuar disponível, para comprovar a transição.

**Descoberta na investigação desta rodada, que muda a premissa do texto acima:** ao investigar este item para finalmente implementá-lo, descobriu-se que `DocumentosEntidade.tsx` (função `slotsDaTela`, constantes `TIPOS_FISCAIS_SIMPLIFICADOS`/`TIPOS_FISCAIS_ECF`) **já tinha**, de alguma rodada anterior não documentada explicitamente como resposta a este item, uma lógica de ocultação de slots por regime confirmado (`slotCompativelComRegime`) -- a mesma situação já registrada acima (item 12) para `classificadorDocumentalCentral.ts`: peça de código real e funcional, mas nunca conectada de volta a este item da lista de pendências. Essa lógica pré-existente, porém, tinha exatamente o problema de regressão que o texto original deste item já antecipava: ela olhava só o regime ATUAL, sem nenhuma memória de mudança de regime -- um documento do Simples já anexado podia (na teoria; não há relato de ter ocorrido em produção) desaparecer da tela assim que o regime fosse confirmado para o outro grupo, e a empresa não tinha como anexar um documento NOVO do regime anterior durante a transição.

**Resolvido nesta rodada** (ver `CHANGELOG_CORRECOES.md`, seção "Rodada 10"), com uma correção mais cirúrgica do que o plano original abaixo (não foi necessário alterar `shared/documentTypes.ts`, porque as constantes `TIPOS_FISCAIS_SIMPLIFICADOS`/`TIPOS_FISCAIS_ECF` já existentes em `DocumentosEntidade.tsx` já cumpriam o papel que o campo `regimesAplicaveis` faria): (a) um slot que já tem documento anexado nunca é escondido, seja qual for o regime confirmado depois; (b) a linha do tempo de regime tributário já persistida (`regimeTributarioTemporalService.ts`, tabela `empresas_regime_tributario_historico`, populada desde uma rodada anterior desta sessão) é anexada ao dossiê e usada para detectar quando a empresa já esteve nos dois grupos fiscais -- nesse caso, os dois grupos ficam visíveis, mesmo para slots ainda não anexados, com um aviso na tela nomeando o regime atual. O comportamento para regime não identificado ou "a confirmar" permanece exatamente como antes desta rodada (mostra tudo/mantém só o grupo ECF-DCTF liberado à parte, respectivamente) -- nenhuma regressão introduzida nesses dois casos.

**Texto original do plano (mantido por transparência histórica; o plano realmente executado foi mais simples, descrito acima e em `CHANGELOG_CORRECOES.md`):** adicionar um campo opcional `regimesAplicaveis?: string[]` a `DocumentCatalogEntry` (`shared/documentTypes.ts`), marcando PGDAS/PGMEI e DEFIS/DASN-SIMEI com `['Simples Nacional', 'MEI / SIMEI']` e ECF/DCTF-DCTFWeb-MIT/Livro Caixa com `['Lucro Presumido', 'Lucro Real', 'Lucro Arbitrado']`; no backend, expor junto com os dados da empresa tanto o regime atual quanto a lista de regimes distintos já registrados na linha do tempo; no frontend, um documento só ficaria oculto quando as três condições fossem verdadeiras ao mesmo tempo (regime confirmado, tipo com `regimesAplicaveis` definido, nem o atual nem o histórico batendo com a lista) -- qualquer incerteza resultaria em MOSTRAR o campo, nunca escondê-lo.

---

Nenhum item desta lista foi omitido por conveniência: cada um está aqui porque implementá-lo com segurança (zero regressão, sem adivinhar dado sensível para decisão de crédito) exige mais do que esta rodada de correção cirúrgica comporta.
